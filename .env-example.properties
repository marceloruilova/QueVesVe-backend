DJANGO_SECRET_KEY=genera-una-clave-secreta-larga-y-aleatoria
DJANGO_DEBUG=True

# Email
# Dev: usa console (imprime en terminal, sin enviar emails reales)
# Prod: smtp envia emails reales via Gmail
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST_USER=tu@gmail.com
EMAIL_HOST_PASSWORD=app-password-de-gmail
BACKEND_BASE_URL=http://localhost:8000

# Postgres
DATABASE_NAME=tiktok_clone
DATABASE_USER=postgres
DATABASE_PASSWORD=tu-password
DATABASE_HOST=localhost
DATABASE_PORT=5432

# CORS y hosts — agregar IP local para probar en dispositivo fisico
# Ejemplo: ALLOWED_HOSTS=localhost,127.0.0.1,192.168.1.100
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:8081,http://localhost:19006

# Tests
TEST_DATABASE_NAME=tiktok_clone_test
TEST_DATABASE_USER=postgres
TEST_DATABASE_PASSWORD=tu-password
TEST_DATABASE_HOST=localhost
TEST_DATABASE_PORT=5432
